import { StyleSheet } from "react-native";

import { _16, _22 } from "../components/constants/sizes";

export const fontClasses = StyleSheet.create({
    baseLine: {
        fontSize: _16,
        lineHeight: _22,
        fontFamily: "Lato-Regular",
    },
});

import { _1, _12, _16, _2, _20, _8 } from "../constants/sizes";
import theme from "../styles/theme";

export const styleConstants = {
    //shadow: `${_2} ${_2} ${_16} rgba(0, 0, 0, 0.15)`,

    shadowRadius: _16,
    shadowOffset: { width: _2, height: _2 },
    shadowColor: theme.black0,
    shadowOpacity: 0.15,

    borderWidth: _1,
    borderRadius_L: _20,
    borderRadius_M: _12,
    borderRadius_S: _8,
};
